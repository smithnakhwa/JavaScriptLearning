function values(x,y=1){
    console.log(x,y);
}
values(10,20);//if the function accept two argument and we set a default value for second and while invoke we pass two values is we not consider default value
values(12);//if the function accept two argument and we pass only one then the default value will be set of the second argumetn.
